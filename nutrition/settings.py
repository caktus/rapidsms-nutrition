# Default settings. If you are not using djappsettings, you will need to
# specify values for each of these settings in your project settings.

# By default, no source is used (that is, queries must be by the global
# identifier).
NUTRITION_PATIENT_HEALTHCARE_SOURCE = None

# By default, no source is used (that is, queries must be by the global
# identifier).
NUTRITION_REPORTER_HEALTHCARE_SOURCE = None
